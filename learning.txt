SASS

.scss --> Use block and curly braces
.sass --> Use indentation

--> Statement

statement {
  statement
}

--> Expression

variable: < expression >
property: < expression >

--> Comments --> Silent commnets in SASS // --> does not produce CSS
--> Single-line comment --> //
--> Multi-line comment --> /* */
/*! This comment will be included even in compressed mode. */

--> Partials --> _partial.scss

--> Extend/Inheritance
--> %placeholder

%message-shared {
  border: 1px solid #ccc;
  padding: 10px;
  color: #333;
}
.message {
  @extend %message-shared;
}
.success {
  @extend %message-shared;
  border-color: green;
}

--> url() function

--> Nesting --> For performance --> Keep Nesting shallow

--> Interpolation

@mixin define-emoji($name, $glyph) {
  span.emoji-#{$name} {
    font-family: IconFont;
    font-variant: normal;
    font-weight: normal;
    content: $glyph;
  }
}
@include define-emoji("women-holding-hands", "👭");

--> @each with List and Map
